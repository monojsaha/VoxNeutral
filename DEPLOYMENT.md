# Deployment Guide

## Prerequisites
- Node.js 18+
- Firebase project (Blaze plan for Firestore)
- Vercel account (free tier works)

## 1. Firebase Setup

### Create Firebase Project
1. Go to [console.firebase.google.com](https://console.firebase.google.com)
2. Create a new project
3. Enable **Authentication** → Email/Password
4. Enable **Firestore Database** → Start in production mode

### Get Client Config
In Project Settings → Your apps → Web app → SDK config:
```
NEXT_PUBLIC_FIREBASE_API_KEY=...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=...
NEXT_PUBLIC_FIREBASE_PROJECT_ID=...
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=...
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=...
NEXT_PUBLIC_FIREBASE_APP_ID=...
```

### Get Admin SDK Config
In Project Settings → Service accounts → Generate new private key:
```
FIREBASE_ADMIN_PROJECT_ID=...
FIREBASE_ADMIN_CLIENT_EMAIL=...
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
```

### Deploy Firestore Rules
```bash
npm install -g firebase-tools
firebase login
firebase use --add   # select your project
firebase deploy --only firestore:rules
```

## 2. Local Development

```bash
cp .env.example .env.local
# Edit .env.local with your Firebase credentials
npm install
npm run dev
```

Visit [http://localhost:3000](http://localhost:3000).

## 3. Vercel Deployment

### One-click deploy
[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new)

### Manual deploy
```bash
npm install -g vercel
vercel login
vercel --prod
```

### Environment Variables in Vercel
Add all variables from `.env.example` in:
Vercel Dashboard → Your Project → Settings → Environment Variables

**Important**: Set `FIREBASE_ADMIN_PRIVATE_KEY` with the value in quotes, including `\n` literals — Vercel will handle newline expansion.

## 4. Type-check and Build

```bash
npm run typecheck    # TypeScript strict check
npm run build        # Production build
npm run test         # Vitest unit tests
```

## 5. Firestore Indexes

The `firestore.indexes.json` file (auto-generated by Firebase CLI) is committed to the repo. To deploy indexes:
```bash
firebase deploy --only firestore:indexes
```

Key indexes needed:
- `users/{uid}/mastery` on `nextReviewAt ASC` (for spaced repetition queries)
- `users/{uid}/attempts` on `timestamp DESC` (for recent attempts)

## 6. Environment Checklist

| Variable | Required | Where |
|----------|----------|-------|
| `NEXT_PUBLIC_FIREBASE_API_KEY` | Yes | Client |
| `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN` | Yes | Client |
| `NEXT_PUBLIC_FIREBASE_PROJECT_ID` | Yes | Client |
| `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET` | Yes | Client |
| `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID` | Yes | Client |
| `NEXT_PUBLIC_FIREBASE_APP_ID` | Yes | Client |
| `FIREBASE_ADMIN_PROJECT_ID` | Server only | Vercel env |
| `FIREBASE_ADMIN_CLIENT_EMAIL` | Server only | Vercel env |
| `FIREBASE_ADMIN_PRIVATE_KEY` | Server only | Vercel env |
